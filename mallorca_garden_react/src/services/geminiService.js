/**
 * Servicio Gemini (Legacy) — Configuración mínima
 * La lógica real de generación ahora se maneja en el backend.
 * Este archivo se mantiene solo por compatibilidad con imports existentes.
 */

/**
 * Verifica si Gemini está configurado (siempre true, el backend lo gestiona)
 */
export function isGeminiConfigured() {
    return true;
}
